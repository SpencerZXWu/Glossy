# Glossy 应用质量检查报告

## 一、基础测试信息

| 项目 | 内容 |
| --- | --- |
| 应用名称 | Glossy（轻量划词翻译工具） |
| 版本号 | v0.2.0 |
| 测试对象 | release 构建：`src-tauri\target\release\glossy.exe`（首轮 7,149,056 字节；修复后复测 7,177,728 字节） |
| 测试环境 | Windows 11；显示器 2560×1600 物理 / 1707×1067 逻辑（缩放 150%）；WebView2 内核 |
| 测试时间 | 2026/9/19（同日晚完成修复并复测） |
| 测试方式 | 真实鼠标操作（拖选、双击、点击、固定、点外部关闭）+ 页面 DOM 实测 + 像素采样 + 接口耗时实测 |
| 测试轮次 | 首轮检查 1 轮 + 修复后复测 2 轮 |
| 测试模块 | 划词触发、翻译与词典、悬浮弹窗、主窗口各面板、运行提示、单实例、主题与语言 |

## 二、测试结果总览

**总体结论：核心划词链路完整可用，无崩溃、无数据丢失；首轮发现的问题 1–4 已全部修复，复测通过。**

| 严重程度 | 首轮数量 | 已修复 | 遗留 |
| --- | --- | --- | --- |
| 严重 | 0 | 0 | 0 |
| 一般 | 3 | 3 | 0 |
| 轻微 | 1 | 1 | 0 |

**修复后复测结论（关键指标）**

| 指标 | 修复前 | 修复后 |
| --- | --- | --- |
| 单词译文出现耗时（`translate_text`） | 24,318–28,261 ms | 266–679 ms |
| 单词卡片出译文（真实双击，端到端） | 24–28 s | 约 1.2 s |
| 音标/释义/增补信息到达 | 与译文同批、常整体超时丢失 | 译文之后单独到达并原卡片合并（最坏 8 s 封顶，实测约 6–8 s） |
| 历史面板刷新 | 需重启应用 | 即时刷新（35 条 → 36 条，首条 `ephemeral` → `resilient`） |
| 历史搜索框样式 | 白底/黑字/圆角 0/高 21.33px | 与其它输入框一致：圆角 9px、内边距 8px 10px、底色 `rgb(20,22,27)` |

**已通过项（实测）**

- 划词触发：在记事本中真机拖选 / 双击英文单词，弹窗均正常弹出，触发稳定。
- 弹窗定位与防溢出：弹窗水平居中于光标下方（光标 425,272 → 弹窗 237,290）；光标移到右下角时弹窗自动收在屏幕内，未越界。
- 弹窗交互：点外部关闭（未固定）✓、固定后点外部不关闭 ✓、关闭按钮 ✓、复制按钮（剪贴板实测为「短暂的」）✓。
- 句子翻译：选中整句 261–321 ms 返回，译文正确，卡片为「原文 / 译文 / 来源」结构。
- 历史记录筛选：搜索过滤、清空、计数文案（`共 N 条` / `匹配 M / N 条`）逻辑正确。
- 单实例：重复启动弹出原生提示框（381×185，标题 Glossy），行为符合设计。
- 运行提示：启动后右下角提示窗可见约 6.5 s（0.7 s → 7.1 s）后自动隐藏，页面渲染正常（深色卡片 `rgb(28,31,38)`、圆角 12px、无文字截断）。
- 主题与语言：浅色/深色两套主题无对比度异常；界面语言切换为英文后无文字截断、无控件溢出。
- 主窗口布局：8 个面板统一 706px 宽、内边距/圆角/背景一致，无重叠、无裁剪、无横向溢出。

## 三、详细问题列表

### 问题 1 ｜ 一般 ｜ 单词查询耗时 24–28 秒，弹窗长时间停在加载态 ｜ ✅ 已修复

- 位置：`src-tauri\src\translate\mod.rs`（`translate()` 中 provider → 词典 → Google 兜底 的串行等待）
- 现象：查询单个英文单词时，弹窗先弹出骨架，随后**长达 24–28 秒**无内容；期间用户会以为程序卡死。
- 实测数据（`translate_text` 端到端计时）：

| 选择内容 | 类型 | 耗时 |
| --- | --- | --- |
| Nice to meet you, see you tomorrow morning. | 句子 | 321 ms |
| ephemeral | 单词 | 26,837 ms |
| hello | 单词 | 28,261 ms |
| 短暂的 | 单词 | 24,318 ms |

- 相关接口实测：`api.dictionaryapi.dev` 0.75–20.2 s 波动（hello / resilient / apple 均约 20 s），`translate.googleapis.com` 在本机不可达（约 21 s 才失败）。单词路径会依次等待这些可能超时的请求，最坏情况叠加约 28 s；句子路径只需一次 provider 请求，所以仅 0.3 s。
- 预期效果：单词卡片应像句子一样在 1 秒内给出译文，词典补充信息异步追加，不应让卡片长时间空转。
- **修复内容**：`translate()` 不再等待词典；新增 `word_details` 命令与 `WordDetails`，弹窗先渲染译文，再单独拉取补充信息并原地合并（`popup.js` 的 `refine()`、`app.js` 的 `refineDemo()`）。
- **复测结果**：`ephemeral` 313–368 ms、`hello` 410–679 ms、句子 266–298 ms；真实双击场景下卡片约 1.2 s 内出现译文。

### 问题 2 ｜ 一般 ｜ 单词卡片常缺少音标、词性、释义、例句 ｜ ✅ 已修复（受上游接口波动影响）

- 位置：`src-tauri\src\translate\dictionary.rs`（`TIMEOUT = 4s`，失败静默忽略）；`mod.rs` 中 `incomplete` 兜底逻辑
- 现象：需求要求「单词/短语显示音标、词性、释义、一个简单例句」，实测经常只剩一行译文。
  - `hello`：无音标、无释义（该接口本次响应 19.9 s，超过 4 s 超时被丢弃，实际该词有 5 条例句可用）。
  - `resilient`：仅显示「坚韧」。
  - `ephemeral`：有音标 `/əˈfɛ.mə.ɹəl/`、noun/adjective 释义，但**无例句**（上游该词确实没有 example 字段）。
- 预期效果：任一字段缺失时应有明确降级策略（例如异步重试、缓存或换源），不要出现「只有译文」的空卡片；例句缺失属于上游数据问题，建议内置备用来源或标注「暂无例句」。
- **修复内容**：词典超时 4 s → 8 s；词典与免费例句源改为**并发**请求，例句源单次请求限时 3 s，任何一路未及时返回都只丢该字段，不再拖慢整张卡片（最坏 8 s 封顶，此前最坏约 32 s）。
- **复测结果**：`ephemeral` 拿到音标 `/əˈfɛ.mə.ɹəl/` 与 noun/adjective 3 条释义，并在卡片出现译文约 6 s 后原地补齐；`hello` 在本机网络下仍只能得到译文（该环境下 `api.dictionaryapi.dev` 触发限流、`translate.googleapis.com` 不可达），属上游限制，应用不再因此空转。

### 问题 3 ｜ 一般 ｜ 历史记录面板不自动刷新，新翻译看不到 ｜ ✅ 已修复

- 位置：`src\js\app.js`（仅在启动、清空、删除时调用 `loadHistory()`，没有监听新翻译事件）；`src-tauri\src\tray.rs`（托盘唤醒只 show/focus，不重新加载）
- 现象：翻译产生的新记录不会出现在「历史记录」面板，必须重启应用才可见。
- 实测数据：同一时刻后台记录 22 条（最新一条为刚查询的 `ephemeral`），面板只显示 21 条，且首条仍是更早的 `resilient`，计数文案为「共 21 条」。
- 预期效果：完成一次翻译后（或主窗口重新显示时）历史列表应自动刷新到最新状态。
- **修复内容**：`translate_text` 完成后由后端 emit `glossy://history`，`app.js` 监听该事件重新拉取历史；补充信息回填历史时同样走 `history::patch_details`。
- **复测结果**：面板 35 条 → 36 条，首条由 `ephemeral` 变为 `resilient`，无需重启。

### 问题 4 ｜ 轻微 ｜ 历史搜索框样式未跟随全局输入框规范 ｜ ✅ 已修复

- 位置：`src\styles\app.css`（共享输入框规则只列了 `select, textarea, input[type="password"], input[type="text"], input[type="number"]`，遗漏 `input[type="search"]`）；`src\index.html` 的 `#historySearch`
- 现象：深色界面中搜索框是浏览器默认样式——白底、黑字、高 21.33px、内边距 1px 2px、圆角 0，而旁边所有输入框均为高 37.48px、内边距 8px 10px、圆角 9px、底色 `rgb(20,22,27)`；两者并排高度不齐、风格割裂（像素采样确认为纯白 `rgb(255,255,255)`）。
- 预期效果：与其它输入框保持同一高度、圆角、底色与内边距。
- **修复内容**：共享输入框规则中加入 `input[type="search"]`（`src\styles\app.css`）。
- **复测结果**：`#historySearch` 与 `#apiKey`、`#historyLimit` 计算样式一致：圆角 9px、边框 `0.667px rgba(255,255,255,0.1)`、内边距 `8px 10px`、底色 `rgb(20,22,27)`。

## 四、优化建议

1. ✅ **已实施：单词路径改为「先出译文、后补详情」**——provider 返回后立即渲染卡片，词典与例句源并发异步补充并原地合并，彻底消除 28 秒空转。
2. ✅ **已实施：调整超时与并发策略**——`dictionary.rs` 超时提升到 8 s，例句源单次请求限时 3 s，两路并发，单字段缺失不再影响整体。
3. ✅ **已实施：历史记录事件通知**——翻译完成后后端 emit `glossy://history`，主窗口即时刷新。
4. ✅ **已实施：补齐输入框样式选择器**——加入 `input[type="search"]`，搜索框与全局规范统一。
5. **建议保留：补充可回归的验证点**——本次未覆盖托盘菜单点击、设置导出/导入、开机自启、历史记录点击回看弹窗、弹窗自动关闭计时；建议在下个版本补充手工回归清单。
6. **建议后续：例句备用来源**——上游例句字段覆盖率低，可考虑本地例句库或第二数据源，并在无例句时给出「暂无例句」占位文案。

**本轮回归验证**：`cargo test` 100 项通过（新增 3 项历史回填用例）、`node --test` 71 项通过、`cargo build --release` 成功；release 构建在真实鼠标双击场景下复测通过。

---

*说明：测试过程中产生了少量测试用历史记录（如 ephemeral、resilient 等）与临时文件，属正常测试数据。*
